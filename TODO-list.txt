Main Game:
1) Make a model loader.

Features:
1) Add DOF distance property.
2) Custom color fog.

3) Make AdvanceStates.

Future Projects:
1) Advance on the math side.
2) Make the render device it's own singular class.
3) Don't only rely on glm.
4) Don't use GL "datavalues." Make my own.
5) Be more dynamic/object orientated and make classes that implements and simplifies library features rather than using the library. 